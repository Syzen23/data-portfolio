export interface Certificate {
  id: string;
  slug: string;
  title: string;
  issuer: string;
  description: string;
  image: string;
  credentialUrl: string;
  createdAt: string;
  updatedAt: string;
}
